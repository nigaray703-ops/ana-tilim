# Ana Tilim ULY 注音显示设计 QA

## 比较目标

- 源视觉真值：`/Users/divenp/Documents/Ana Tilim/artifacts/uly-transliteration/source-option-1.png`
- 浏览器实现截图：`/Users/divenp/Documents/Ana Tilim/artifacts/uly-transliteration/desktop-aa-option-1-952x908.jpg`
- 同屏比较证据：`/Users/divenp/Documents/Ana Tilim/artifacts/uly-transliteration/comparison-option-1-952x908.jpg`
- 移动端证据：`/Users/divenp/Documents/Ana Tilim/artifacts/uly-transliteration/mobile-khe-viewport-390x844.jpg`
- 页面：`http://127.0.0.1:4173/prototype/?refresh=rerecord-audio-14`

## 尺寸与状态

- 源图像素：1580 × 995。
- 桌面实现：CSS 视口 952 × 908，DPR 1；截图 952 × 908。
- 移动实现：CSS 视口 390 × 844，DPR 1；截图 390 × 844。
- 同屏比较图：1280 × 720；源视觉和桌面实现都按比例装入独立画板，只用于观察层级、内容顺序和整体视觉关系，不据此做像素级尺寸判断。
- 状态：第一单元 `ئا / ئە` 字母页，显示拉丁转写开启，`ئا` 为当前字母。
- 交互验证：进入“我的”页面；关闭“显示拉丁转写”；刷新并确认隐藏状态持久化；重新开启并确认恢复显示；进入词汇和句子页面检查 ULY；确认已删除“字体大小”设置。
- 控制台：检查 `error` 与 `warn`，无相关日志。

## 全视图比较

- 信息层级一致：写法标签、字母形态、维吾尔语例词、ULY、中文释义按相同顺序呈现。
- 结构一致：4 种写法均采用 2 × 2 卡片网格；形态区使用浅绿色底；例词中目标字母仍以红色突出。
- 实现保留了完整课程页的导航、进度、发音与练习入口，因此整体密度高于单独的源组件；这是产品上下文差异，不改变源视觉指定的例词内部层级。

## 聚焦区域比较

聚焦检查了“4 种位置写法”组件。四个例词及其 ULY/中文顺序均与源视觉一致：

- `ئانا / ana / 妈妈`
- `قارا / qara / 黑色`
- `ئالما / alma / 苹果`
- `خەلقئارا / xelq'ara / 国际`

该区域文字清晰，无截断、重叠或错误换行。

## 必查表面

- 字体与排版：维吾尔语保持 Times New Roman 600；ULY 使用较小、低对比度的左到右文本；中文释义权重更低。层级清楚，长句允许换行且未被裁切。
- 间距与布局：桌面 2 × 2 网格、卡片内边距、浅绿色形态区及三层文本节奏与源视觉一致。桌面与移动视口均未发现内容遮挡。
- 颜色与视觉标记：沿用项目的深蓝、青绿色和米白色令牌；红色目标字母、浅绿色形态区、灰蓝 ULY 均符合已选方案。
- 图像与资源质量：对比区域没有新增插图或替代资产；现有 Ana Tilim 标志保持清晰，没有拉伸、光晕或占位图。
- 文案与内容：四个示例词、ULY 和中文释义与已确认方案一致；设置项文案为“显示拉丁转写”，并准确说明覆盖字母、词语和句子。
- 图标与控件：导航、收藏和发音控件延续项目现有图标体系，大小与对齐一致。
- 状态与交互：ULY 开关可切换并持久化；练习未作答时不显示答案型转写提示；删除字体大小设置后其余设置仍可用。
- 可访问性与响应式：开关使用语义按钮并反映按下状态；移动端目标内容无水平溢出；维吾尔语与 ULY 分别保持正确书写方向。

## Findings

- 无 P0、P1 或 P2 问题。
- [P3] 源视觉是独立组件，浏览器实现位于完整课程页中，所以组件视觉尺寸更紧凑。课程页需要同时容纳导航、字母形态和练习入口，该差异属于已接受的产品约束。

## 比较历史

- 第 1 次：同屏比较未发现可执行的 P0/P1/P2 差异，因此无需因本次视觉比较修改实现。
- 用户后续要求删除“字体大小”功能；该设置及相关交互和样式已移除，并通过设置页 DOM、截图和自动化测试复核。这是需求变更，不是视觉比较缺陷。

## 剩余测试范围

- 已自动检查 32 个字母、34 个组合、209 个词汇、164 条阅读内容、126 个写法例词和 464 个渲染状态。
- 已在浏览器中抽查字母、词汇、长句和设置页；未逐一截图全部内容，因为 ULY 由同一标准化模块生成，并由全量数据测试覆盖。

final result: passed
